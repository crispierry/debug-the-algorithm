{
  "name": "debug-the-algorithm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:qa": "npm run build && netlify deploy --no-build --dir=dist --alias=qa-preview --message \"QA deploy\"",
    "deploy:prod": "npm run build && netlify deploy --no-build --dir=dist --prod --message \"Production deploy\""
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
